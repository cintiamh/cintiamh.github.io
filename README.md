# cintiamh.github.io

Main page for personal github.

To run locally:

```
$ jekyll serve
$ docker-compose up
```

You should be able to access on http://localhost:4000

When done, run:

```
$ jekyll build
```